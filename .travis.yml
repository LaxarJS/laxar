language: node_js
sudo: false
node_js:
   - "4"
   - "0.12"
notifications:
   irc:
      channels: "chat.freenode.net#laxarjs"
      template: "%{repository_slug} (%{commit}) [%{branch}]: %{message}"
branches:
   except:
      - /^feature[^\/]*\/.*$/
